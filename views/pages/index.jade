extends ../layout

block content
  .container
    .row
      each item in music
        //-.col-md-2
          //-.thumbnail
            //-a(href="/music/#")
              //-img(src="#{item.poster}", alt="#{item.title}")
            //-.caption
              //-h3 #{title}
              //-p: a.btn.btn-primary(href="/music/#", role="button")
                播放
